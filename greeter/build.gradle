apply plugin : 'java' 
apply plugin : 'application' 

mainClassName = 'greeter.Greeter' 

dependencies {
    compile project(':greeting-lib') 
}

distZip {
    from project(':docs').asciidoctor, { 
        into "${project.name}-${version}"
    }
}
distTar {
    from project(':docs').asciidoctor, {
        into "${project.name}-${version}"
    }
}
